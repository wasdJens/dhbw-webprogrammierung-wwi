In this folder you find an example for commonjs and esmodules format which you can both use in nodejs.

The "esm" format is indicated by the file type `.mjs`

## Run
```shell
node index.js

node index.mjs
```